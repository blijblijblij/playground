events {
  worker_connections  1024;
}

http {
  server {
    auth_basic "Restricted";
    auth_basic_user_file /etc/nginx/passwords;

    location / {
      proxy_pass http://webspoon:8080/spoon;
      proxy_redirect off;
    }
  }
}
